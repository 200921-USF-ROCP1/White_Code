-- Cascasde keyword

-- drop and other commands have two modes
--		cascade & restricted (default)

drop table apartments cascade; -- remove constraints when table is dropped


select * from residents r ;
select * from pets;
